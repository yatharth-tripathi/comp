import { SipBuilder } from "@/components/illustrations/sip-builder";

export const metadata = { title: "SIP · PitchWiz" };

export default function SipPage(): JSX.Element {
  return <SipBuilder />;
}
