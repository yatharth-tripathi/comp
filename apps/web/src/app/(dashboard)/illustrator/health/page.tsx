import { HealthInsuranceBuilder } from "@/components/illustrations/health-builder";

export const metadata = { title: "Health Insurance · PitchWiz" };

export default function HealthPage(): JSX.Element {
  return <HealthInsuranceBuilder />;
}
