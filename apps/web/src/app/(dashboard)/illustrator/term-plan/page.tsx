import { TermPlanBuilder } from "@/components/illustrations/term-plan-builder";

export const metadata = { title: "Term Insurance · PitchWiz" };

export default function TermPlanPage(): JSX.Element {
  return <TermPlanBuilder />;
}
